# 1. 现有模块
## 1.1 API被淘汰，作为经典用法
这部分API放到了 langchain-classic 包
如 Memory 模块，现在的记忆管理完全可以用Langgraph提供的InmemoryServer结合中间件替代
## 直接找平替，大模块列表格，说明一下迁移策略，小模块一语带过
## **确认是真淘汰还是换路径**
**寻找替代品**
## 1.2 API仍在用，但不是主推
包的导入路径没变，仍然可用，但是不是官方推荐的重点，官方找不到，如PromptTemplate只能在langchain-core的API文档中查到
### Runnable
### Prompt

**代码无需更改，考虑笔记是否弱化**
## 1.3 仍然是核心模块
**无需更改**

# 2. 新增模块
## 2.1 中间件
Middleware
### 案例
1. **动态切换模型**
    @wrap_model_call 中间件
        自定义实现逻辑

2. **上下文压缩**
    裁剪
        @before_model
            自定义实现逻辑
            返回的Messages字段下用RemoveMessage标识要删除的消息，再新增保留的消息
    删除
        @after_model
            前后应该都可以
            用RemoveMessage
    摘要
        SummarizationMiddleware
3. **人在环**
    HumanInTheLoopMiddleware
        需要部署看效果
## 2.2 DeepAgent


## 2.3 SKILLS
之前讲过了MCP，但没有提SKILLS，是否要加？

## 2.4 Multi-Agent 多智能体

# 3. 其它框架
1. 对接LangSmith
2. 结合Langgraph部署
3. 是否要把Langgraph一起讲了？
4. 是否要梳理几个框架之间的关系